import { Todo } from '../types/todo';
import { TODO_STATUSES, TODO_CATEGORIES } from '../constants';

export const sampleTodos: Todo[] = [
  
];

// Cette fonction est maintenant obsolète, remplacée par le store